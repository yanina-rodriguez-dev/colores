import React from 'react';
import { ListGroup } from 'react-bootstrap';

const ListaColores = () => {
    return (
        <ListGroup>
            
        </ListGroup>
    );
};

export default ListaColores;